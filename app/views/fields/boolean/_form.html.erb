<%#
# Boolean Form Partial

This partial renders an input element for a boolean attribute.
By default, the input is a checkbox.

## Local variables:

- `f`:
  A Rails form generator, used to help create the appropriate input fields.
- `field`:
  An instance of [Administrate::Field::Boolean][1].
  A wrapper around the boolean value pulled from the database.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Field/Boolean
%>

<% errors[field.attribute.to_sym].each do |error| %>
  <span class="nhsuk-error-message" id="input-with-error-message-error">
    <span class="nhsuk-u-visually-hidden">Error:</span><%= error %>
  </span>
<% end %>
<div class="nhsuk-checkboxes">
  <div class="nhsuk-checkboxes__item">
    <%= f.check_box field.attribute, class: "nhsuk-checkboxes__input" %>
    <%= f.label field.attribute, class: "nhsuk-label nhsuk-checkboxes__label" %>
  </div>
</div>
